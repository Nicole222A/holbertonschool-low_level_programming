#include <stdio.h>
/**
 * main -  Write a C program that prints a message
 * followed by a new line
 * Return: always 0
 **/
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
